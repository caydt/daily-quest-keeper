# Map Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/map` 페이지에서 농장과 나무를 RPG 월드맵 스타일로 시각화하고, 클릭 시 메인 화면 해당 항목으로 스크롤 이동한다.

**Architecture:** 세 파일 변경. FarmCard/ProjectCard에 id 속성 추가 → index.tsx에서 ?scroll= 파라미터 감지해 scrollIntoView → map.tsx 새 라우트에서 useGarden() 읽어 farms/projects/tasks를 시각화. 별도 상태·API 없음.

**Tech Stack:** React 19, TanStack Router (file-based routing), Tailwind CSS, treeStage/farmStage 기존 유틸 재사용

---

### Task 1: FarmCard / ProjectCard에 id 속성 추가

**Files:**
- Modify: `src/components/garden/ProjectList.tsx`

- [ ] **Step 1: FarmCard 루트 div에 id 추가**

`ProjectList.tsx`의 FarmCard 반환부 최상단 div를 찾는다 (현재 `className="rounded-3xl border border-emerald-500/20 ..."`). `id` prop을 추가한다:

```tsx
<div id={`farm-${farm.id}`} className="rounded-3xl border border-emerald-500/20 bg-emerald-950/20 overflow-hidden">
```

- [ ] **Step 2: ProjectCard 루트 div에 id 추가**

ProjectCard 반환부 최상단 div (`className="group relative rounded-2xl border-2 ..."`):

```tsx
<div
  id={`project-${p.id}`}
  ref={setNodeRef}
  style={style}
  className={`group relative rounded-2xl border-2 transition-all ${...}`}
>
```

- [ ] **Step 3: 빌드 확인**

```bash
cd /Users/ayoungjo/daily-quest-keeper && npm run build 2>&1 | tail -5
```

Expected: `✓ built in` 으로 끝남

- [ ] **Step 4: 커밋**

```bash
cd /Users/ayoungjo/daily-quest-keeper && git add src/components/garden/ProjectList.tsx && git commit -m "feat: FarmCard/ProjectCard에 scroll 대상 id 속성 추가"
```

---

### Task 2: index.tsx — scroll 파라미터 처리 + 헤더 맵 버튼

**Files:**
- Modify: `src/routes/index.tsx`

- [ ] **Step 1: Map 아이콘 import 추가**

파일 상단 lucide-react import에 `Map` 추가:

```tsx
import { Settings as SettingsIcon, BarChart3, Bell, BellOff, Wrench, Sparkles, Map } from "lucide-react";
```

- [ ] **Step 2: scroll 파라미터 처리 useEffect 추가**

`Index` 컴포넌트 내부, `useReminders` 호출 바로 아래에 추가:

```tsx
// ?scroll=farm-xxx 또는 ?scroll=project-xxx 파라미터 처리
useEffect(() => {
  if (!hydrated) return;
  const params = new URLSearchParams(window.location.search);
  const scrollTo = params.get("scroll");
  if (!scrollTo) return;
  // DOM 렌더 후 실행
  const timer = setTimeout(() => {
    const el = document.getElementById(scrollTo);
    if (!el) return;
    el.scrollIntoView({ behavior: "smooth", block: "center" });
    el.style.transition = "outline 0.1s";
    el.style.outline = "2px solid hsl(var(--primary))";
    el.style.outlineOffset = "4px";
    setTimeout(() => {
      el.style.outline = "";
      el.style.outlineOffset = "";
    }, 2000);
    // URL에서 파라미터 제거 (뒤로가기 시 재실행 방지)
    const url = new URL(window.location.href);
    url.searchParams.delete("scroll");
    window.history.replaceState({}, "", url.toString());
  }, 300);
  return () => clearTimeout(timer);
}, [hydrated]);
```

- [ ] **Step 3: 헤더에 🗺️ 맵 버튼 추가**

헤더의 `<div className="flex items-center gap-2">` 안, 주간 회고 Link 바로 아래에 추가:

```tsx
<Link
  to="/map"
  className="inline-flex items-center gap-1.5 px-3 py-2 rounded-xl border border-white/10 bg-card/60 hover:border-primary/40 text-xs font-medium text-muted-foreground hover:text-primary transition"
>
  <Map className="size-3.5" /> <span className="hidden sm:inline">맵</span>
</Link>
```

- [ ] **Step 4: 빌드 확인**

```bash
cd /Users/ayoungjo/daily-quest-keeper && npm run build 2>&1 | tail -5
```

Expected: `✓ built in` 으로 끝남

- [ ] **Step 5: 커밋**

```bash
cd /Users/ayoungjo/daily-quest-keeper && git add src/routes/index.tsx && git commit -m "feat: 맵 페이지 이동 버튼 + scroll 파라미터 처리"
```

---

### Task 3: map.tsx — 맵 페이지 생성

**Files:**
- Create: `src/routes/map.tsx`

- [ ] **Step 1: 파일 생성**

`src/routes/map.tsx`를 아래 전체 내용으로 생성한다:

```tsx
import { createFileRoute, Link } from "@tanstack/react-router";
import { useGarden, treeStage, farmStage } from "@/lib/garden-store";
import { ArrowLeft } from "lucide-react";

export const Route = createFileRoute("/map")({
  head: () => ({
    meta: [{ title: "세계 지도 — 루미 가든" }],
  }),
  component: MapPage,
});

function MapPage() {
  const { state, hydrated } = useGarden();

  if (!hydrated) return <div className="min-h-dvh" />;

  const farms = [...state.farms].sort((a, b) => (a.order ?? 0) - (b.order ?? 0));

  if (farms.length === 0) {
    return (
      <div className="min-h-dvh flex flex-col items-center justify-center gap-4 text-center px-6">
        <p className="text-5xl">🗺️</p>
        <p className="text-lg font-semibold">아직 농장이 없어요</p>
        <p className="text-sm text-muted-foreground">메인 화면에서 농장을 만들면 여기에 세계가 펼쳐집니다.</p>
        <Link
          to="/"
          className="inline-flex items-center gap-2 px-4 py-2 rounded-xl bg-primary/15 border border-primary/40 text-primary text-sm font-semibold hover:bg-primary/25 transition"
        >
          <ArrowLeft className="size-4" /> 정원으로
        </Link>
      </div>
    );
  }

  return (
    <div
      className="min-h-dvh relative"
      style={{
        backgroundImage:
          "radial-gradient(circle, rgba(255,255,255,0.04) 1px, transparent 1px)",
        backgroundSize: "28px 28px",
      }}
    >
      {/* 헤더 */}
      <div className="sticky top-0 z-10 flex items-center justify-between px-6 py-4 border-b border-white/5 backdrop-blur-md bg-background/70">
        <Link
          to="/"
          className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-primary transition"
        >
          <ArrowLeft className="size-4" /> 정원으로
        </Link>
        <h1 className="font-display text-lg text-gradient-gold">🗺️ 세계 지도</h1>
        <p className="text-xs text-muted-foreground">{farms.length}개 농장</p>
      </div>

      {/* 맵 캔버스 */}
      <div className="p-8 md:p-12">
        <div
          className="grid gap-8"
          style={{ gridTemplateColumns: "repeat(auto-fill, minmax(320px, 1fr))" }}
        >
          {farms.map((farm, idx) => {
            const trees = state.projects
              .filter((p) => p.farmId === farm.id)
              .sort((a, b) => (a.order ?? 0) - (b.order ?? 0));

            const treeStats = trees.map((p) => {
              const childTasks = state.tasks.filter((t) => t.projectId === p.id);
              const total = childTasks.length;
              const done = childTasks.filter((t) => t.completed).length;
              return total === 0 ? (p.completed ? 100 : 0) : (done / total) * 100;
            });
            const avgPct =
              treeStats.length === 0
                ? 0
                : treeStats.reduce((a, b) => a + b, 0) / treeStats.length;
            const stage = farmStage(trees.length, avgPct);

            // 홀짝 행마다 살짝 offset으로 RPG 지도 느낌
            const offset = idx % 2 === 1 ? "mt-6" : "";

            return (
              <div key={farm.id} className={offset}>
                <FarmTerritory
                  farm={farm}
                  trees={trees}
                  tasks={state.tasks}
                  stage={stage}
                  avgPct={avgPct}
                />
              </div>
            );
          })}
        </div>
      </div>
    </div>
  );
}

function FarmTerritory({
  farm,
  trees,
  tasks,
  stage,
  avgPct,
}: {
  farm: import("@/lib/garden-store").Farm;
  trees: import("@/lib/garden-store").Project[];
  tasks: import("@/lib/garden-store").Task[];
  stage: { icon: string; label: string };
  avgPct: number;
}) {
  return (
    <Link
      to="/"
      search={{ scroll: `farm-${farm.id}` } as never}
      className="block rounded-3xl border-2 border-emerald-500/30 bg-emerald-950/25 hover:border-emerald-400/50 hover:bg-emerald-950/40 transition-all group"
    >
      {/* 농장 헤더 */}
      <div className="px-5 pt-5 pb-3 flex items-center gap-3">
        <span className="text-3xl">{stage.icon}</span>
        <div className="flex-1 min-w-0">
          <div className="flex items-center gap-2">
            <h2 className="font-bold text-emerald-300 truncate">{farm.title}</h2>
            <span className="text-[10px] px-1.5 py-0.5 rounded-full bg-emerald-500/10 text-emerald-400 border border-emerald-500/20 shrink-0">
              {stage.label}
            </span>
          </div>
          <p className="text-[11px] text-muted-foreground mt-0.5">
            나무 {trees.length}그루 · 평균 {Math.round(avgPct)}%
          </p>
        </div>
        {farm.icon && (
          <span className="text-xl opacity-60 group-hover:opacity-100 transition-opacity">
            {farm.icon}
          </span>
        )}
      </div>

      {/* 진행 바 */}
      <div className="px-5 pb-3">
        <div className="h-1 rounded-full bg-black/30 overflow-hidden">
          <div
            className="h-full rounded-full bg-gradient-to-r from-emerald-500 to-emerald-300 transition-all"
            style={{ width: `${avgPct}%` }}
          />
        </div>
      </div>

      {/* 나무 노드들 */}
      <div className="px-5 pb-5">
        {trees.length === 0 ? (
          <p className="text-xs text-muted-foreground/50 italic text-center py-4">
            아직 나무가 없어요
          </p>
        ) : (
          <div className="flex flex-wrap gap-3">
            {trees.map((tree) => (
              <TreeNode key={tree.id} tree={tree} tasks={tasks} farmId={farm.id} />
            ))}
          </div>
        )}
      </div>
    </Link>
  );
}

function TreeNode({
  tree,
  tasks,
}: {
  tree: import("@/lib/garden-store").Project;
  tasks: import("@/lib/garden-store").Task[];
  farmId: string;
}) {
  const childTasks = tasks.filter((t) => t.projectId === tree.id);
  const total = childTasks.length;
  const done = childTasks.filter((t) => t.completed).length;
  const pct = total === 0 ? (tree.completed ? 100 : 0) : (done / total) * 100;
  const stage = treeStage(pct, tree.completed);

  return (
    <Link
      to="/"
      search={{ scroll: `project-${tree.id}` } as never}
      onClick={(e) => e.stopPropagation()}
      className={`flex flex-col items-center gap-1 w-16 group/tree transition-opacity ${
        tree.completed ? "opacity-50" : ""
      }`}
      title={tree.title}
    >
      {/* 원형 아이콘 */}
      <div
        className={`size-14 rounded-full flex items-center justify-center text-2xl border-2 transition-all ${
          tree.completed
            ? "border-primary/30 bg-primary/10"
            : "border-accent/30 bg-card/60 group-hover/tree:border-accent/70 group-hover/tree:bg-accent/10"
        }`}
        style={{
          boxShadow: pct > 0 && !tree.completed
            ? `0 0 ${Math.round(pct / 10) * 2}px rgba(var(--accent), 0.3)`
            : undefined,
        }}
      >
        {stage.icon}
      </div>

      {/* 제목 */}
      <p className="text-[10px] text-center text-muted-foreground leading-tight line-clamp-2 w-full group-hover/tree:text-foreground transition-colors">
        {tree.title}
      </p>

      {/* 진행률 */}
      {total > 0 && (
        <p className="text-[9px] text-primary/70 tabular-nums">
          {done}/{total}
        </p>
      )}
    </Link>
  );
}
```

- [ ] **Step 2: TanStack Router search params 타입 문제 확인**

`search={{ scroll: ... } as never}` 로 타입을 우회했으므로 빌드에서 타입 에러 여부를 확인한다:

```bash
cd /Users/ayoungjo/daily-quest-keeper && npm run build 2>&1 | grep -i "error\|warn" | head -20
```

타입 에러 발생 시: `Link to="/" search` 대신 `href` 방식으로 교체:

```tsx
// FarmTerritory의 Link를 div + useNavigate 방식으로:
import { useNavigate } from "@tanstack/react-router";

// FarmTerritory 내부:
const navigate = useNavigate();
const handleClick = () => navigate({ to: "/", search: {} as never }).then(() => {
  setTimeout(() => {
    const el = document.getElementById(`farm-${farm.id}`);
    el?.scrollIntoView({ behavior: "smooth", block: "center" });
  }, 300);
});
```

에러 없으면 그대로 진행.

- [ ] **Step 3: 빌드 최종 확인**

```bash
cd /Users/ayoungjo/daily-quest-keeper && npm run build 2>&1 | tail -5
```

Expected: `✓ built in` 으로 끝남

- [ ] **Step 4: 커밋 + 푸시**

```bash
cd /Users/ayoungjo/daily-quest-keeper && git add src/routes/map.tsx docs/superpowers/plans/2026-04-27-map-page.md && git commit -m "feat: RPG 월드맵 페이지 (/map) 추가" && git push origin main
```
